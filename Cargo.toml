[package]
name = "breezydb"
version = "0.1.0"
edition = "2024"

[dependencies]
crc32c = "0.6"
rand = "0.10.2"
libc = "0.2.189"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
debug = 1
